`print()` writes to standard output. Whatever sits between the parentheses is evaluated
and displayed, and text has to be quoted so Python knows it is text rather than a name.

It is the simplest way to see what a program is actually doing, which is why it stays
useful long after it stops being the interesting part.
