Predict the output before running it.

`range(n)` is half-open: it starts at 0 and stops *before* n. Getting this wrong once is
normal; getting it wrong repeatedly is the thing this step is watching for.
