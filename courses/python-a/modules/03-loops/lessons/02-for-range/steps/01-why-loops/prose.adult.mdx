Ten `print()` calls to show ten numbers is ten chances to make a typo, and it does not
scale to a hundred.

A `for` loop runs the same block once for each item a sequence hands it. `range(10)`
produces the numbers 0 through 9 — note where it starts, and where it stops.
